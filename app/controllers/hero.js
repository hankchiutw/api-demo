'use strict';

const cn = require('co-nextware');

/**
 * Expose
 */

module.exports = {
    list: cn(list),
    oneById: cn(oneById)
};

function *list(req, res, next){
}

function *oneById(req, res, next){
}
